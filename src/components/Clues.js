import React from 'react';

export default class Clues extends React.Component {
  // constructor(props) {
  //   super(props)
  // }

  toIndex(row, column) {
    return row * 5 + column
  }

  render() {


  return (
    1
  )
  }
}
