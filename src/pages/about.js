import React from 'react';

const About = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'Right',
        alignItems: 'Right',
        height: '100vh'
      }}
    >
      <h3>
      Nonograms are a logic puzzle also known as Picross, Griddlers, and many other names. The idea is to fill in boxes in a grid based on clues to create a hidden picture.
      </h3>
    </div>
  );
};

export default About;
